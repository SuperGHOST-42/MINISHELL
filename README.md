# MINISHELL
A "small" UNIX shell implementation written in C, recreating essential features of Bash. It reads and parses user input, handles pipes, redirections, environment variables, and executes both built-in and external commands — while correctly managing processes, file descriptors, and signals.
